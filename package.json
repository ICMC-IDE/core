{
  "name": "core",
  "version": "1.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build --verbose tsconfig.build.json"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
